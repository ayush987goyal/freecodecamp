var React = require('react'),
    {
      Grid,
      Row,
      Col,
    } = require('react-bootstrap');

var Nonprofits = React.createClass({

  render: function () {
    return (
      <h1>Hello Non Profits!</h1>
    );
  }
});

module.exports = Nonprofits;
